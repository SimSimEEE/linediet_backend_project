# Swagger API 테스트 가이드

## 📋 목차
1. [테스트 준비](#테스트-준비)
2. [테스트 순서](#테스트-순서)
3. [상세 테스트 케이스](#상세-테스트-케이스)

---

## 테스트 준비

### 1. 서버 실행
```bash
npm run dev
```

### 2. Swagger UI 접속
브라우저에서 다음 URL 접속:
```
http://localhost:3000/api-docs
```

### 3. 환경 확인
- 서버가 정상적으로 실행되었는지 확인
- Swagger UI에서 모든 API 엔드포인트가 보이는지 확인

---

## 테스트 순서

API 간 의존성을 고려한 권장 테스트 순서입니다.

```
1. Doctor API (의사 등록)
   └─→ 2. Patient API (환자 등록)
        └─→ 3. Appointment API (예약 생성)
             └─→ 4. Visit API (진료 기록)
```

---

## 상세 테스트 케이스

### 🩺 Phase 1: Doctor API 테스트

#### 1-1. 의사 생성 (POST /doctors)
**목적**: 테스트에 사용할 의사 2명 생성

**Test Case 1: 김한의 의사 생성**
```json
{
  "name": "김한의",
  "specialization": "한방내과",
  "licenseNumber": "12345",
  "isActive": true,
  "notes": "경력 10년"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `doctorId1` = response.id

**Test Case 2: 이침구 의사 생성**
```json
{
  "name": "이침구",
  "specialization": "침구과",
  "licenseNumber": "67890",
  "isActive": true,
  "notes": "침술 전문"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `doctorId2` = response.id

**Test Case 3: 유효성 검증 - 이름 누락**
```json
{
  "specialization": "한방내과"
}
```
❌ **예상 결과**: 400 Bad Request

---

#### 1-2. 의사 조회 (GET /doctors/{id})
**목적**: 생성된 의사 정보 확인

**Test Case 1: 김한의 의사 조회**
- URL: `/doctors/{doctorId1}`
- ✅ **예상 결과**: 200 OK
- 응답에 "김한의" 포함

**Test Case 2: 존재하지 않는 의사 조회**
- URL: `/doctors/invalid-id`
- ❌ **예상 결과**: 404 Not Found

---

#### 1-3. 의사 목록 조회 (GET /doctors)
**목적**: 전체 의사 목록 확인

**Test Case 1: 전체 목록**
- URL: `/doctors`
- ✅ **예상 결과**: 200 OK
- items 배열에 최소 2개 이상

**Test Case 2: 활성 의사만 조회**
- URL: `/doctors?activeOnly=true`
- ✅ **예상 결과**: 200 OK
- 모든 items의 isActive = true

**Test Case 3: 제한된 개수 조회**
- URL: `/doctors?limit=1`
- ✅ **예상 결과**: 200 OK
- items 배열 길이 = 1

---

#### 1-4. 의사 검색 (POST /doctors/search)
**목적**: 이름으로 의사 검색

**Test Case 1: 이름으로 검색**
```json
{
  "name": "김한의"
}
```
✅ **예상 결과**: 200 OK, items에 "김한의" 포함

**Test Case 2: 부분 검색**
```json
{
  "name": "김"
}
```
✅ **예상 결과**: 200 OK, items에 "김한의" 포함

**Test Case 3: 검색어 누락**
```json
{}
```
❌ **예상 결과**: 400 Bad Request

---

#### 1-5. 의사 정보 수정 (PUT /doctors/{id})
**목적**: 의사 정보 업데이트

**Test Case 1: 전문 분야 변경**
- URL: `/doctors/{doctorId1}`
```json
{
  "specialization": "한방내과 및 재활의학과",
  "notes": "경력 15년으로 업데이트"
}
```
✅ **예상 결과**: 200 OK

**Test Case 2: 의사 비활성화**
- URL: `/doctors/{doctorId2}`
```json
{
  "isActive": false
}
```
✅ **예상 결과**: 200 OK

---

#### 1-6. 의사 삭제 (DELETE /doctors/{id})
**목적**: Soft delete 테스트

⚠️ **주의**: 나중에 다시 생성하거나, 삭제하지 않고 넘어가세요.

**Test Case 1: 의사 삭제**
- URL: `/doctors/{doctorId2}`
- ✅ **예상 결과**: 204 No Content

**Test Case 2: 삭제된 의사 조회**
- URL: `/doctors/{doctorId2}`
- ❌ **예상 결과**: 404 Not Found

---

### 👤 Phase 2: Patient API 테스트

#### 2-1. 환자 생성 (POST /patients)
**목적**: 테스트에 사용할 환자 2명 생성

**Test Case 1: 홍길동 환자 생성**
```json
{
  "name": "홍길동",
  "phoneNumber": "01012345678",
  "birthYearMonth": "199001",
  "ssn": "9001011234567",
  "notes": "고혈압 주의"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `patientId1` = response.id

**Test Case 2: 김영희 환자 생성**
```json
{
  "name": "김영희",
  "phoneNumber": "01098765432",
  "birthYearMonth": "198505",
  "notes": "알레르기 있음"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `patientId2` = response.id

**Test Case 3: 유효성 검증 - 필수 필드 누락**
```json
{
  "name": "테스트"
}
```
❌ **예상 결과**: 400 Bad Request

---

#### 2-2. 환자 조회 (GET /patients/{id})
**목적**: 생성된 환자 정보 확인

**Test Case 1: 홍길동 환자 조회**
- URL: `/patients/{patientId1}`
- ✅ **예상 결과**: 200 OK
- 📝 **확인**: phoneNumber가 복호화되어 반환됨

---

#### 2-3. 환자 목록 조회 (GET /patients)
**Test Case 1: 전체 목록**
- URL: `/patients`
- ✅ **예상 결과**: 200 OK

**Test Case 2: 제한된 개수**
- URL: `/patients?limit=1`
- ✅ **예상 결과**: 200 OK

---

#### 2-4. 환자 검색 (POST /patients/search)
**목적**: 이름/전화번호로 검색

**Test Case 1: 이름으로 검색**
```json
{
  "name": "홍길동"
}
```
✅ **예상 결과**: 200 OK, items에 "홍길동" 포함

**Test Case 2: 전화번호로 검색**
```json
{
  "phoneNumber": "01012345678"
}
```
✅ **예상 결과**: 200 OK, items에 "홍길동" 포함

---

#### 2-5. 환자 정보 수정 (PUT /patients/{id})
**Test Case 1: 메모 업데이트**
- URL: `/patients/{patientId1}`
```json
{
  "notes": "고혈압 약 복용 중"
}
```
✅ **예상 결과**: 200 OK

---

#### 2-6. 환자 삭제 (DELETE /patients/{id})
⚠️ **주의**: 예약/진료 테스트를 위해 삭제하지 마세요!

---

### 📅 Phase 3: Appointment API 테스트

#### 3-1. 예약 생성 (POST /appointments)
**목적**: 다양한 예약 시나리오 테스트

**Test Case 1: 기존 환자 예약**
```json
{
  "doctorId": "{doctorId1}",
  "patientId": "{patientId1}",
  "bookerName": "홍길동",
  "bookerPhone": "01012345678",
  "appointmentDate": "2025-12-01",
  "appointmentTime": "14:00"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `appointmentId1` = response.id

**Test Case 2: 신규 환자 예약 (patientId 없음)**
```json
{
  "doctorId": "{doctorId1}",
  "bookerName": "박민수",
  "bookerPhone": "01055556666",
  "appointmentDate": "2025-12-01",
  "appointmentTime": "15:00"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `appointmentId2` = response.id

**Test Case 3: 유효성 검증 - 과거 시간**
```json
{
  "doctorId": "{doctorId1}",
  "bookerName": "테스트",
  "bookerPhone": "01011112222",
  "appointmentDate": "2020-01-01",
  "appointmentTime": "10:00"
}
```
❌ **예상 결과**: 400 Bad Request

**Test Case 4: 유효성 검증 - 잘못된 시간 형식**
```json
{
  "doctorId": "{doctorId1}",
  "bookerName": "테스트",
  "bookerPhone": "01011112222",
  "appointmentDate": "2025-12-01",
  "appointmentTime": "14:15"
}
```
❌ **예상 결과**: 400 Bad Request (00:00 또는 00:30만 허용)

**Test Case 5: 중복 예약**
```json
{
  "doctorId": "{doctorId1}",
  "bookerName": "테스트",
  "bookerPhone": "01011112222",
  "appointmentDate": "2025-12-01",
  "appointmentTime": "14:00"
}
```
❌ **예상 결과**: 409 Conflict

---

#### 3-2. 예약 조회 (GET /appointments)
**목적**: 날짜/의사별 예약 조회

**Test Case 1: 날짜로 조회**
- URL: `/appointments?appointmentDate=2025-12-01`
- ✅ **예상 결과**: 200 OK, list에 2개 이상

**Test Case 2: 의사별 조회**
- URL: `/appointments?appointmentDate=2025-12-01&doctorId={doctorId1}`
- ✅ **예상 결과**: 200 OK

**Test Case 3: 필수 파라미터 누락**
- URL: `/appointments`
- ❌ **예상 결과**: 400 Bad Request

---

#### 3-3. 예약 검색 (POST /appointments/search)
**목적**: 예약자 정보로 검색

**Test Case 1: 이름과 전화번호로 검색**
```json
{
  "bookerName": "홍길동",
  "bookerPhone": "01012345678"
}
```
✅ **예상 결과**: 200 OK, list에 홍길동 예약 포함

---

#### 3-4. 예약 수정 (PUT /appointments/{id})
**Test Case 1: 시간 변경**
- URL: `/appointments/{appointmentId1}`
```json
{
  "appointmentTime": "16:00"
}
```
✅ **예상 결과**: 200 OK

---

#### 3-5. 예약 취소 (POST /appointments/{id}/cancel)
**Test Case 1: 예약 취소**
- URL: `/appointments/{appointmentId2}/cancel`
```json
{
  "reason": "개인 사정"
}
```
✅ **예상 결과**: 200 OK

**Test Case 2: 이미 취소된 예약 재취소**
- URL: `/appointments/{appointmentId2}/cancel`
```json
{
  "reason": "다시 취소"
}
```
✅ **예상 결과**: 200 OK (이미 취소됨)

---

### 🏥 Phase 4: Visit API 테스트

#### 4-1. 진료 생성 (POST /visits)
**목적**: 체크인 및 진료 시작

**Test Case 1: 기존 환자 진료**
```json
{
  "patientId": "{patientId1}",
  "doctorId": "{doctorId1}",
  "appointmentId": "{appointmentId1}",
  "visitType": "follow-up",
  "chiefComplaint": "허리 통증"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `visitId1` = response.id

**Test Case 2: 신규 환자 진료 (환자 정보 포함)**
```json
{
  "doctorId": "{doctorId1}",
  "patientName": "최신규",
  "phoneNumber": "01077778888",
  "ssn": "9505051234567",
  "birthYearMonth": "199505",
  "visitType": "first",
  "chiefComplaint": "두통"
}
```
✅ **예상 결과**: 201 Created
📝 **저장**: `visitId2` = response.id

**Test Case 3: 유효성 검증 - 의사 ID 누락**
```json
{
  "patientId": "{patientId1}",
  "chiefComplaint": "복통"
}
```
❌ **예상 결과**: 400 Bad Request

**Test Case 4: 신규 환자 정보 불충분**
```json
{
  "doctorId": "{doctorId1}",
  "visitType": "first",
  "chiefComplaint": "두통"
}
```
❌ **예상 결과**: 400 Bad Request

---

#### 4-2. 진료 조회 (GET /visits/{id})
**Test Case 1: 진료 내역 조회**
- URL: `/visits/{visitId1}`
- ✅ **예상 결과**: 200 OK
- 📝 **확인**: patient$, doctor$ 정보 포함

---

#### 4-3. 환자별 진료 목록 (GET /visits/patient/{patientId})
**Test Case 1: 특정 환자 진료 목록**
- URL: `/visits/patient/{patientId1}`
- ✅ **예상 결과**: 200 OK, items에 최소 1개

**Test Case 2: 제한된 개수**
- URL: `/visits/patient/{patientId1}?limit=1`
- ✅ **예상 결과**: 200 OK

---

#### 4-4. 진료 검색 (POST /visits/search)
**목적**: 날짜 범위로 검색

**Test Case 1: 날짜 범위 검색**
```json
{
  "patientId": "{patientId1}",
  "startDate": "2025-01-01T00:00:00+09:00",
  "endDate": "2025-12-31T23:59:59+09:00"
}
```
✅ **예상 결과**: 200 OK

**Test Case 2: 필수 파라미터 누락**
```json
{
  "patientId": "{patientId1}"
}
```
❌ **예상 결과**: 400 Bad Request

---

#### 4-5. 진료 내용 수정 (PUT /visits/{id})
**Test Case 1: 진단 추가**
- URL: `/visits/{visitId1}`
```json
{
  "diagnosis": "요추 염좌",
  "notes": "침 치료 필요"
}
```
✅ **예상 결과**: 200 OK

---

#### 4-6. 진료 완료 (POST /visits/{id}/complete)
**목적**: 진료 종료 및 처방 기록

**Test Case 1: 진료 완료**
- URL: `/visits/{visitId1}/complete`
```json
{
  "diagnosis": "요추 염좌",
  "treatment": "침 치료, 한약 처방 (7일분)",
  "notes": "1주일 후 재진"
}
```
✅ **예상 결과**: 200 OK
📝 **확인**: completedAt 필드 추가됨

**Test Case 2: 존재하지 않는 진료**
- URL: `/visits/invalid-id/complete`
```json
{
  "diagnosis": "테스트",
  "treatment": "테스트"
}
```
❌ **예상 결과**: 404 Not Found

---

## 🎯 통합 시나리오 테스트

전체 플로우를 순서대로 테스트:

### 시나리오 1: 예약 환자 진료 플로우
```
1. 의사 생성 (김한의)
   ↓
2. 환자 생성 (홍길동)
   ↓
3. 예약 생성 (2025-12-01 14:00)
   ↓
4. 체크인 (Visit 생성)
   ↓
5. 진료 완료 (진단/처방 기록)
```

### 시나리오 2: 신규 환자 워크인 플로우
```
1. 의사 생성 (김한의)
   ↓
2. 신규 환자 체크인 (환자 정보 + Visit 동시 생성)
   ↓
3. 진료 완료
   ↓
4. 생성된 환자 ID로 다음 예약 생성
```

---

## 📝 테스트 체크리스트

### Phase 1: Doctor API
- [ ] 의사 2명 생성
- [ ] 의사 조회 성공
- [ ] 의사 목록 조회
- [ ] 의사 검색 (이름)
- [ ] 의사 정보 수정
- [ ] 유효성 검증 테스트

### Phase 2: Patient API
- [ ] 환자 2명 생성
- [ ] 환자 조회 (복호화 확인)
- [ ] 환자 목록 조회
- [ ] 환자 검색 (이름/전화번호)
- [ ] 환자 정보 수정
- [ ] 유효성 검증 테스트

### Phase 3: Appointment API
- [ ] 기존 환자 예약 생성
- [ ] 신규 환자 예약 생성
- [ ] 중복 예약 차단
- [ ] 과거 시간 예약 차단
- [ ] 시간 형식 검증
- [ ] 예약 조회 (날짜/의사)
- [ ] 예약 검색 (예약자 정보)
- [ ] 예약 수정
- [ ] 예약 취소

### Phase 4: Visit API
- [ ] 기존 환자 진료 생성
- [ ] 신규 환자 진료 생성
- [ ] 진료 조회 (populate 확인)
- [ ] 환자별 진료 목록
- [ ] 날짜 범위 검색
- [ ] 진료 내용 수정
- [ ] 진료 완료
- [ ] 유효성 검증 테스트

---

## 💡 테스트 팁

### 1. ID 관리
- 각 생성 요청의 응답에서 ID를 복사해두세요
- 텍스트 편집기에 ID를 정리해두면 편리합니다
- 예: `doctorId1 = doctor-1234567890-abc123`

### 2. 날짜/시간 설정
- `appointmentDate`: 미래 날짜 사용 (예: 2025-12-01)
- `appointmentTime`: 00:00 또는 00:30 형식만 허용
- 검색 시 ISO 형식 사용: `2025-01-01T00:00:00+09:00`

### 3. 암호화 필드 확인
- `phoneNumber`, `ssn`은 암호화되어 저장됨
- 조회 시 자동으로 복호화되어 반환됨
- 검색 시 원본 값으로 검색 가능

### 4. Soft Delete 확인
- DELETE 후 GET 요청 시 404 반환
- 하지만 데이터베이스에는 존재 (deletedAt 필드 설정)

### 5. 에러 코드
- `400`: 유효성 검증 실패
- `404`: 리소스 없음
- `409`: 중복/충돌
- `500`: 서버 에러

---

## 🔍 문제 해결

### Q1: 과거 시간 예약이 성공함
- 현재 시간보다 미래 날짜/시간을 사용하세요
- 예: 2025-12-01 이후 날짜

### Q2: 예약 시간 형식 에러
- 14:00, 14:30만 가능
- 14:15, 14:45는 불가능

### Q3: 환자 검색이 안 됨
- 정확한 이름 또는 전화번호 사용
- 전화번호는 하이픈 없이 숫자만 (예: 01012345678)

### Q4: 진료 생성 시 환자 정보 에러
- 신규 환자: patientName, phoneNumber, ssn 필수
- 기존 환자: patientId만 필요

---

## 📊 테스트 결과 기록

테스트 진행하면서 체크:

| API | 테스트 케이스 | 성공 | 실패 | 비고 |
|-----|-------------|------|------|------|
| POST /doctors | 의사 생성 | ✅ | | |
| GET /doctors/{id} | 의사 조회 | | | |
| POST /patients | 환자 생성 | | | |
| POST /appointments | 예약 생성 | | | |
| POST /visits | 진료 생성 | | | |
| ... | ... | | | |

---

## 🎓 학습 포인트

이 테스트를 통해 확인할 수 있는 것들:

1. **RESTful API 설계**: HTTP 메서드와 상태 코드의 적절한 사용
2. **데이터 유효성 검증**: 필수 필드, 형식 검증
3. **비즈니스 로직**: 중복 방지, 시간 검증 등
4. **데이터 보안**: 개인정보 암호화/복호화
5. **관계 데이터 처리**: Populate를 통한 연관 데이터 조회
6. **에러 핸들링**: 적절한 에러 메시지와 상태 코드

---

**Happy Testing! 🚀**
